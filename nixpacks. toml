[phases.setup]
nixPkgs = ["python3", "python311Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install pytelegrambotapi flask openai gunicorn"]

[start]
cmd = "gunicorn main:app"
